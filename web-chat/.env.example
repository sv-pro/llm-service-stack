# API Configuration
REACT_APP_API_URL=http://localhost:3000/api

# API Key (Optional for localhost development)
# Leave empty if app-server has ALLOW_LOCALHOST_BYPASS=true
# For production or if bypass is disabled, generate a key with:
#   bash scripts/create-demo-api-key.sh
REACT_APP_API_KEY=
